{
  "buildCommand": "cd realtime-doc-editor && npm install && CI=false npm run build",
  "outputDirectory": "realtime-doc-editor/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}